#include <string>
#include <cstdlib>
#include <array>
#include "../include/map.h"
#include <iostream>

int main()
{
  if (mapGrid.size() == 65536) {return EXIT_SUCCESS;}
  else {return EXIT_FAILURE;}
}
